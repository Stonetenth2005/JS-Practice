"use strict"; // treats all the code in newer version

// alert(3+3)// this is not allowed in Node JS, since we are not using any document for this, we can use this in developer console in browser

/*

data types

number => Range - 2^53
bigint => large integer value
string => ""
boolean => true/false
undefined => value not assigned yet, not even null till now
symbol => to find uniqueness

objects -

null => standalone value (empty)

*/

console.log(typeof(null)) // returns object
console.log(typeof(undefined)) // returns undefined